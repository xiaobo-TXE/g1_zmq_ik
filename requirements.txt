# 用 uv 安装（推荐）：
#   uv venv --python 3.10
#   uv pip install -r requirements.txt
# 或者直接读 pyproject.toml：
#   uv sync
numpy>=1.26
pyzmq>=24
pin>=3.1
casadi>=3.6

# --config 读 .toml 用：Python 3.11+ 用标准库 tomllib，3.10 用它的前身 tomli（只读，无传递依赖）
tomli>=2.0; python_version < "3.11"

# 说明：
# * PyPI 的 pin 轮子含 Pinocchio，但【不含 pinocchio.casadi】。程序会自动改用内置符号正解
#   （同一份 URDF 数据搭 CasADi 表达式，与 pinocchio.casadi 数值差 ~2e-16，见 README）。
# * PyPI 的 casadi 轮子自带 IPOPT 插件（libcasadi_nlpsol_ipopt.so），所以 IPOPT 不需要额外安装。
# * 若确实想用 pinocchio.casadi 这条上游原路径，只能用 conda-forge：
#       conda install -c conda-forge pinocchio casadi
#
# 可选（只有 tools/detect_aruco_zmq.py 需要；主程序不需要）：
#   opencv-contrib-python>=4.7     # cv2.aruco 在 contrib 包里
